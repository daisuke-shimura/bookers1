<h2>Editing Book</h2>


<% if @hon.errors.any? %>

  <div class="message-box">　<%= @hon.errors.count %> errors prohibited this book from being saved:
  <ul>
    <% @hon.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
</div>

<%= form_with model: @book do |f| %>
  <div class="New-book">
    <h4>Title</h4>
    <%= f.text_field :title %>

    <h4>Body</h4>
    <%= f.text_area :body %>

    <div class="submit">
      <%= f.submit "Update Book" %>
    </div>
  </div>

  <div class="swich">
    <string class="botan"><%= link_to "Show", book_path(@book.id)%></string>
    <%= link_to "Back", '/books' %>
  </div>
<% end %>
